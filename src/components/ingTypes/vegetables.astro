---
import { db } from '../../lib/db';


const vegetales = await db
  .selectFrom('ingredients')
  .selectAll()
  .where('ingType_ID', '=', 1)
  .execute();

  console.log(vegetales);
---

<form method="GET" action="/menu" class="max-w-md mx-auto mt-6">
  <div class="bg-white rounded-xl shadow-lg p-6">
    <h2 class="text-xl font-semibold mb-4 text-gray-800 text-center">Selecciona tus vegetales</h2>
    <ul class="space-y-3">
      {vegetales.length > 0 ? (
        vegetales.map(veg => (
          <li key={veg.ingredient_ID}>
            <label class="flex items-center space-x-3">
              <input type="checkbox" name="vegetales" value={veg.ingredient_ID} class="accent-green-600 h-4 w-4 rounded" />
              <span class="text-gray-700">{veg.nombre_Ingrediente}</span>
            </label>
          </li>
        ))
      ) : (
        <li class="text-gray-500 text-center">No hay vegetales disponibles.</li>
      )}
    </ul>
    <button
      type="submit"
      class="mt-6 w-full bg-purple-600 hover:bg-purple-700 text-white px-5 py-2 rounded-full font-medium transition"
    >
      Generar menú con vegetales seleccionados
    </button>
  </div>
</form>